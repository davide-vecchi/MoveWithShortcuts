
# MoveWithShortcuts artifact Build List.
# (REPOS subfolder : "FileTools\MoveWithShortcuts\").


[Initialization]

# For Windows :

pwsh -Command "Copy-Item C:\MoveWithShortcuts\MoveWithShortcuts.jar ('C:\MoveWithShortcuts\MoveWithShortcuts-' + (Get-Date -Format 'yyyy-MM-dd_HH-mm-ss') + '.jar')"

cmd.exe /c DIR C:\MoveWithShortcuts\MoveWithShortcuts*.jar /O-D

# cmd.exe /c IF EXIST "C:\Users\Davide\.m2\repository\djavalibraries" (RMDIR /S /Q "C:\Users\Davide\.m2\repository\djavalibraries")
# cmd.exe /c IF EXIST "C:\Users\Davide\.m2\repository\javalibraries3rdparty\threadsafenumberformat" (RMDIR /S /Q "C:\Users\Davide\.m2\repository\javalibraries3rdparty\threadsafenumberformat")

# For Unix :

# cp /home/user/MoveWithShortcuts/MoveWithShortcuts.jar /home/user/MoveWithShortcuts/CopyFile-$(date +'%Y-%m-%d_%H-%M-%S').jar
# /bin/sh -c "ls -t /home/user/MoveWithShortcuts/MoveWithShortcuts*.jar"

# /bin/sh -c 'rm -rf "/home/davide/.m2/repository/djavalibraries"'
# /bin/sh -c 'rm -rf "/home/davide/.m2/repository/javalibraries3rdparty/threadsafenumberformat"'




[Options]

# This section may contain or not the following line (case-insensitive) :
#
# NoPause


[Modules]

#
# Beginning of the module list.
#
# Each module is represented by a block made of 2 or 3 lines :
#
#   The 1st line is the filesystem path to the module's folder,
#   that is the folder where the pom.xml is.
#
#   The 2nd line is the build command to issue for that module;
#   it can have any arguments.
#
#   The 3nd line, if present, is the path of the folder where the artifact built
#   for that module must be moved. If the line is not present, the artifact will
#   not be moved.
#
#   There can not be any comment or empty line inside a block.
#   There must be at least one empty line between blocks.
#

# Build the MoveWithShortcuts module, as an executable jar, and deploy it :

H:\Users\Davide\Davide\Sviluppo\REPOS\FileTools\MoveWithShortcuts
mvn clean install
C:\MoveWithShortcuts
